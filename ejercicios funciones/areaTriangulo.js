// function areaTriangulo(base, altura) {
//     // procesos a realizar
// }

// areaTriangulo();


// Implementación con función de flecha
const areaTriangulo = (base, altura) => {
    console.log("El área del triángulo con base " + base + " y altura " + altura + " es: " + ((base * altura) / 2));
}

areaTriangulo(10, 5);
